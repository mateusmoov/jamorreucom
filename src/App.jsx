import { useState } from "react";
import { Home, NotFound, Results } from "./pages";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return <Results />;
}

export default App;
